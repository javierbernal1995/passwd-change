---
- name: Create user
  user:
    name: bernal
    password: "{{ 'bernal' | password_hash('sha512') }}"
    shell: /bin/bash
    update_password: on_create
  register: bernal
- name: Force to change password
  shell: chage -d 0 bernal
  when: bernal.changed
