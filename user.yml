---
-hosts: 'grupo'
vars:
  user_name:'javi'
  user_name: 'bernal'
tasks:
-name: 'Change your passwd'
become: true
user:
  name: '{{ user_name }}'
  password: '{{ new_password | password_hash("sha512)"}}'
  update_password: 'always'
